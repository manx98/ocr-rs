name: build-prebuilt

# Builds prebuilt/<os>_<arch>/libocr_rs_combined.a for every supported target
# and uploads them as workflow artifacts. Manual trigger only.
on:
  workflow_dispatch:

jobs:
  # ---------------------------------------------------------------------------
  # Linux: build inside an Ubuntu 18.04 docker container so the resulting
  # archive only needs glibc 2.27 at runtime.
  #
  # We deliberately do NOT use the workflow's `container:` field, because the
  # runner injects a node20 binary that requires GLIBC_2.28 — which 18.04
  # does not have. Instead keep all GitHub action helpers (checkout,
  # upload-artifact) on the host runner, and only the cargo build runs in
  # the ubuntu:18.04 container. Same docker image works for both arches —
  # ubuntu:18.04 publishes amd64 and arm64 manifests.
  # ---------------------------------------------------------------------------
  linux:
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - goarch: amd64
            runner: ubuntu-latest
          - goarch: arm64
            runner: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4
      - name: Build inside ubuntu:18.04
        run: |
          set -eux
          docker run --rm \
            -v "$PWD":/work -w /work \
            -e DEBIAN_FRONTEND=noninteractive \
            ubuntu:18.04 bash -c '
              set -eux
              apt-get update
              apt-get install -y --no-install-recommends \
                  ca-certificates curl git build-essential pkg-config \
                  software-properties-common gnupg \
                  clang libclang-dev \
                  ocl-icd-opencl-dev libvulkan-dev
              # cmake >= 3.18 from Kitware (default 18.04 cmake is 3.10)
              curl -fsSL https://apt.kitware.com/keys/kitware-archive-latest.asc \
                  | gpg --dearmor -o /usr/share/keyrings/kitware-archive.gpg
              echo "deb [signed-by=/usr/share/keyrings/kitware-archive.gpg] https://apt.kitware.com/ubuntu/ bionic main" \
                  > /etc/apt/sources.list.d/kitware.list
              apt-get update
              apt-get install -y --no-install-recommends cmake
              # gcc-8 for C++17 in MNN (default 18.04 gcc is 7)
              add-apt-repository -y ppa:ubuntu-toolchain-r/test
              apt-get update
              apt-get install -y --no-install-recommends gcc-8 g++-8
              update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-8 80 \
                  --slave /usr/bin/g++ g++ /usr/bin/g++-8
              # rustup
              curl -fsSL https://sh.rustup.rs | sh -s -- -y --profile minimal --default-toolchain stable
              export PATH="$HOME/.cargo/bin:$PATH"
              bash scripts/build_linux.sh
            '
      - name: Verify archive arch
        run: file "prebuilt/linux_${{ matrix.goarch }}/libocr_rs_combined.a" | head -3
      - uses: actions/upload-artifact@v4
        with:
          name: prebuilt-linux_${{ matrix.goarch }}
          path: prebuilt/linux_${{ matrix.goarch }}/libocr_rs_combined.a

  # ---------------------------------------------------------------------------
  # macOS: both arch variants are built on macos-14 (arm host).
  #
  # The arm64 target builds natively against /opt/homebrew bottles. The
  # x86_64 target cross-compiles under Rosetta and links against a
  # parallel x86_64 Homebrew install at /usr/local — that's the only way
  # to get an x86_64 libvulkan.dylib now that macos-13 free runners are
  # gone.
  #
  # ocr-rs's side cdylib pulls -lvulkan into the link line; Homebrew's
  # vulkan-loader + molten-vk satisfy it at build time. CPU/OpenCL/Metal
  # /CoreML do not need any extra install.
  # ---------------------------------------------------------------------------
  macos:
    runs-on: macos-14
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-apple-darwin
            goarch: amd64
          - target: aarch64-apple-darwin
            goarch: arm64
    steps:
      - uses: actions/checkout@v4
      - name: Install Rosetta 2 (x86_64 target only)
        if: matrix.target == 'x86_64-apple-darwin'
        run: softwareupdate --install-rosetta --agree-to-license
      - name: Install Vulkan loader (build-time only)
        run: |
          set -eux
          if [[ "${{ matrix.target }}" == "x86_64-apple-darwin" ]]; then
              # Bootstrap an x86_64 Homebrew at /usr/local so brew bottles
              # are x86_64 Mach-O, then install vulkan there.
              if [ ! -x /usr/local/bin/brew ]; then
                  arch -x86_64 /bin/bash -c \
                      "NONINTERACTIVE=1 $(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
              fi
              arch -x86_64 /usr/local/bin/brew update --quiet
              arch -x86_64 /usr/local/bin/brew install vulkan-headers vulkan-loader molten-vk
              echo "LIBRARY_PATH=/usr/local/lib:${LIBRARY_PATH:-}" >> $GITHUB_ENV
              echo "CPATH=/usr/local/include:${CPATH:-}" >> $GITHUB_ENV
          else
              brew update --quiet
              brew install vulkan-headers vulkan-loader molten-vk
              brew_prefix="$(brew --prefix)"
              echo "LIBRARY_PATH=${brew_prefix}/lib:${LIBRARY_PATH:-}" >> $GITHUB_ENV
              echo "CPATH=${brew_prefix}/include:${CPATH:-}" >> $GITHUB_ENV
          fi
      - name: Install Rust toolchain
        run: |
          rustup toolchain install stable --profile minimal
          rustup target add ${{ matrix.target }}
      - name: Build
        env:
          OCRRS_TARGET: ${{ matrix.target }}
        run: bash scripts/build_macos.sh
      - name: Verify archive arch
        run: |
          file "prebuilt/darwin_${{ matrix.goarch }}/libocr_rs_combined.a" | head -3
      - uses: actions/upload-artifact@v4
        with:
          name: prebuilt-darwin_${{ matrix.goarch }}
          path: prebuilt/darwin_${{ matrix.goarch }}/libocr_rs_combined.a

  # ---------------------------------------------------------------------------
  # Collect every per-platform artifact into prebuilt/, commit it back to the
  # branch the workflow was dispatched from, and also upload a combined
  # workflow artifact for download.
  # ---------------------------------------------------------------------------
  bundle:
    needs: [linux, macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          # The default GITHUB_TOKEN is enough to push to the same repo
          # provided `permissions.contents: write` is granted above.
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ github.ref }}
          # `actions/checkout` defaults to a shallow clone; we need full
          # history so the push is a fast-forward.
          fetch-depth: 0
      - uses: actions/download-artifact@v4
        with:
          pattern: prebuilt-*
          path: prebuilt-dl
      - name: Lay out into prebuilt/
        run: |
          set -eux
          for d in prebuilt-dl/prebuilt-*; do
              name="${d##*/prebuilt-}"
              mkdir -p "prebuilt/$name"
              # Each artifact dir contains a single libocr_rs_combined.a
              # (.lib on Windows). Replace the whole directory contents so
              # stale .gitkeep placeholders get cleaned up.
              rm -f "prebuilt/$name/.gitkeep"
              cp "$d"/* "prebuilt/$name/"
          done
          ls -la prebuilt/*/
      - name: Commit refreshed archives back to ${{ github.ref_name }}
        env:
          BRANCH: ${{ github.ref_name }}
        run: |
          set -eux
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add prebuilt/
          if git diff --cached --quiet; then
              echo "No prebuilt changes — skipping commit."
              exit 0
          fi
          git commit -m "ci: refresh prebuilt archives [skip ci]"
          git push origin "HEAD:${BRANCH}"
      - uses: actions/upload-artifact@v4
        with:
          name: prebuilt-all
          path: prebuilt/**/libocr_rs_combined.a
